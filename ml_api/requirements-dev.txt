# Test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# Then run the suite from the ml_api/ directory:
#   pytest
-r requirements.txt
pytest==8.4.0
httpx==0.28.1
